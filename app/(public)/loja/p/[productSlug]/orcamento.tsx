/**
 * /loja/p/:productSlug/orcamento — Quote request (hostname-mode).
 * Re-exports the [tenantSlug]/[productSlug]/orcamento screen for hostname-resolved tenants.
 */
export { default } from "../../[tenantSlug]/[productSlug]/orcamento";
